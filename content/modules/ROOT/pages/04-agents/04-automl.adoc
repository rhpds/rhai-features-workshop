= Advanced: Predictive AI with AutoML
:navtitle: AutoML (Advanced)
:imagesdir: ../assets/images
:toc:
:toc-placement: preamble
:icons: font

WARNING: This is an advanced optional section. It requires additional time and is intended for participants who want to understand how the Risk MCP's predictive model was trained. The main section exercises are complete — this section is self-contained and can be done independently.

== Where Does the Prediction Come From?

In Exercise 4, you watched {assistant_name} call `uw_predict_loan_approval` through the Risk MCP server. The response was not a rule — it was a machine learning prediction. But where does that prediction come from?

Behind the `uw_predict_loan_approval` tool is a scikit-learn classification model trained on historical loan application data. The model learned which combinations of DTI ratio, LTV, credit score, and asset sufficiency correlate with loan outcomes, and it applies that learning to new applications at inference time.

Training that model by hand — selecting algorithms, tuning hyperparameters, tracking experiments — would take days. AutoML automates the entire process. It runs multiple algorithm configurations in parallel, compares the results, and surfaces the best-performing model — all logged to MLflow so every run is reproducible and auditable.

This is how {company_name} connects classical machine learning to generative AI: the LLM orchestrates and decides what to ask, the ML model predicts, and AutoML keeps that model accurate as new loan data comes in.

== Learning Objectives

By the end of this section, you will be able to:

* Run an AutoML training job using pre-prepared notebook collateral
* Review the resulting experiment in MLflow and compare model runs
* Understand how the trained model connects to the Risk MCP server

== Exercise: Train the Loan Approval Model

. Open your Workbench: *OpenShift AI Dashboard > Data Science Projects > wksp-{user} > Workbench*.

. In the file browser, navigate to the `automl/` directory. Open `automl_training.ipynb`:
+
// TODO: screenshot of automl_training.ipynb open in Workbench (automl-notebook-open.png)
image::automl-notebook-open.png[automl_training.ipynb open in the Workbench showing the training configuration cells,link=self,window=blank,width=700]

. Review the first few cells. The notebook:
+
* Loads a synthetic dataset of historical loan applications with known outcomes
* Defines a set of algorithm candidates (logistic regression, random forest, gradient boosting)
* Configures MLflow to log every run automatically

. Run all cells. The training job will take a few minutes. As it runs, each algorithm configuration is evaluated and logged as a separate run in MLflow:
+
// TODO: screenshot of notebook cells executing with MLflow logging output (automl-notebook-running.png)
image::automl-notebook-running.png[Notebook output showing MLflow run logging as AutoML training progresses,link=self,window=blank,width=700]

. Once training completes, navigate to *OpenShift AI Dashboard > Develop & Train > Experiments* and find the experiment named `automl-loan-approval`. You will see multiple runs — one per algorithm configuration — with their accuracy, precision, and recall scores:
+
// TODO: screenshot of automl-loan-approval experiment in MLflow (automl-mlflow-experiment.png)
image::automl-mlflow-experiment.png[MLflow automl-loan-approval experiment showing multiple algorithm runs with metrics,link=self,window=blank,width=700]

. Click on the run marked as best (or the one with the highest accuracy). Review the parameters — algorithm, hyperparameters — and the metrics. MLflow captures the full lineage: what data went in, what configuration was used, what results came out:
+
// TODO: screenshot of best run detail in MLflow (automl-mlflow-best-run.png)
image::automl-mlflow-best-run.png[MLflow best run detail showing algorithm configuration and accuracy metrics,link=self,window=blank,width=700]

. The best model is automatically registered in the *MLflow Model Registry*. Navigate to *Model Registry* and find `loan-approval-classifier`. The latest version is the model you just trained:
+
// TODO: screenshot of model registry entry (automl-model-registry.png)
image::automl-model-registry.png[MLflow Model Registry showing the loan-approval-classifier with the newly registered version,link=self,window=blank,width=700]

. The Risk MCP server is already configured to load the latest version of `loan-approval-classifier` from the Model Registry at startup. Your newly trained model is now what {assistant_name} calls when she runs `uw_predict_loan_approval` on a new application.

TIP: In production, this training pipeline would run on a schedule — weekly or monthly — as new loan outcome data arrives. AutoML ensures the model stays calibrated without requiring a data scientist to manually tune it each time.

Reference: https://github.com/rh-aiservices-bu/fed-aura-capital-rhai-workshop/tree/dev/templates/user/automl[AutoML collateral on GitHub,window=blank]

== Section Summary

=== What You Accomplished

* Ran an AutoML training job that evaluated multiple algorithm configurations against historical loan application data
* Reviewed the resulting MLflow experiment and compared run metrics to identify the best-performing model
* Registered the best model in the MLflow Model Registry and confirmed that the Risk MCP server loads it automatically

=== Key Takeaways

* AutoML reduces the time from raw data to deployed model — algorithm selection, hyperparameter tuning, and experiment tracking are automated rather than manual
* MLflow provides full lineage for every run: data, configuration, metrics, and the resulting model artifact are all linked and auditable
* The connection between AutoML and the MCP server is the Model Registry — the MCP server loads whatever version is marked latest, so retraining and redeploying the model requires no changes to the agent code
* Classical ML and generative AI are complementary: the LLM handles language, reasoning, and orchestration; the ML model handles structured prediction tasks that require statistical pattern recognition
