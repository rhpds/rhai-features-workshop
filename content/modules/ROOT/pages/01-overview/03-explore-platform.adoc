= Explore the Platform
:imagesdir: ../assets/images
:navtitle: Explore the Platform
:toc: macro
:toclevels: 1

You have the background. The CTO told you why the platform was rebuilt. The previous page showed you the four pillars and the architecture. Now it is time to see it running and interact with it yourself.

== Learning objectives

By the end of this section, you will be able to:

* Log in to OpenShift and verify that all platform components are running
* Confirm API and database health using the health endpoint
* Interact with {assistant_name} as different personas and observe how responses change based on role
* Connect running pods to the platform architecture diagram

== Exercise 1: Access the environment and verify the deployment

TIP: Throughout this workshop, all key services are accessible as *tabs* in the workshop interface - no need to open separate browser windows.

. First, log in to the OpenShift cluster from the **Terminal** tab:
+
[source,console,role=execute,subs=attributes+]
----
oc login --insecure-skip-tls-verify $(oc whoami --show-server) -u {user} -p {password}
----

. Navigate to the *OCP Console* tab. Log in with the same credentials you just used: username `{user}`, password `{password}`:
+
image::agentic-app-pre1.png[OpenShift SSO login page,link=self,window=blank,width=500]

. Once logged in, you will see the `Projects` page and a single project: `wksp-{user}`. Navigate to **Workloads > Pods** to see the platform components running in your namespace:
+
image::workloads-pods.png[]
+
If you do not see pods, make sure you select your project:
+
image::select-project.png[]
image::agentic-app0.png[OpenShift Console Pods view showing mortgage-ai components running,link=self,window=blank,width=700]

. Check the API service health endpoint:
+
[source,console,role=execute,subs=attributes+]
----
MORTGAGE_HEALTH=$(oc get route mortgage-ai-api-health-route -n wksp-{user} -o jsonpath='{.spec.host}')
curl -sk https://$\{MORTGAGE_HEALTH}/health/ | jq -r .
----
+
Expected output:
+
[source,json]
----
[
  {
    "name": "API",
    "status": "healthy",
    "message": "API is running",
    "version": "0.1.0",
    "start_time": "2026-04-07T22:21:27.657181+00:00"
  },
  {
    "name": "Database",
    "status": "healthy",
    "message": "PostgreSQL connection successful",
    "version": "0.1.0",
    "start_time": "2026-04-07T22:21:19.877102+00:00"
  }
]
----
+
**What this result means:** Both the API and Database components report `"status": "healthy"`. This confirms the FastAPI pod is running and can connect to PostgreSQL. Without a healthy database connection, the agents cannot access their knowledge base, retrieve application data, or execute RAG workflows. If either component shows a status other than `"healthy"`, wait a minute and try again - the deployment may still be starting up.

=== Network and security context

NOTE: Your namespace includes NetworkPolicies that restrict east-west traffic between pods. The mortgage-ai-api pod can reach mortgage-ai-db and minio, but not pods in other namespaces. In section 5.3 (OpenShell Agent Sandboxing), you will explore OpenShell sandboxing, which adds application-level security on top of these network boundaries.

[%collapsible]
.View the NetworkPolicies in your namespace (click to expand)
====
[source,console,role=execute,subs=attributes+]
----
oc get networkpolicies -n wksp-{user}
----

NetworkPolicies define which pods can communicate with each other at the network level. This is the first layer of the defense-in-depth security model you will explore in section 5.
====

== Exercise 2: Meet {assistant_name} and the agent personas

Now that you have confirmed the platform is healthy, open the application and interact with {assistant_name} through different personas. Each persona connects to a different agent endpoint with different tools and permissions. The same question produces different responses depending on who is asking.

=== Try the Prospect experience (unauthenticated)

. Navigate to the *{company_name} App* tab:
+
image::mortgage-app.png[]

. Scroll down a bit. Try a sample conversation by clicking the **Explore Products** button:
+
image::explore-products.png[Explore Products button on the {company_name} landing page,link=self,window=blank,width=400]

. In the chat panel, ask {assistant_name} about mortgage products:
+
[source,text]
----
What mortgage products do you offer? I'd like to compare my options.
----
+
The Prospect Agent should respond with mortgage product options:
+
image::module5-prospect-products.png[{assistant_name} responding with mortgage product options,link=self,window=blank,width=700]
+
Remember this question - you will ask it again in section 3 under different configurations to see how the answer changes when you add system prompts and RAG.

NOTE: The LLM backend has rate limiting and may occasionally time out. If the agent does not respond or you see an error, reload the page and ask the question again.

NOTE: Because the application uses generative AI, responses are non-deterministic. The exact wording, formatting, and details may differ from the screenshots shown in this workshop.

=== Try the Borrower experience (Sarah Mitchell)

[start=4]
. From the application landing page, click **Sign In**:
+
image::sign-in.png[]

. In the sign-in dialog, use the **Persona Demo Login** section at the bottom and select the **Borrower** persona. Click **Sign In**:
+
image::agentic-app4.png[Sign in dialog with Persona Demo Login,link=self,window=blank,width=700]
// TODO: capture screenshot showing Borrower selection specifically (explore-borrower-login.png)

. In the chat panel, ask {assistant_name} a policy question:
+
[source,text]
----
What is the minimum down payment for an FHA loan?
----
+
Observe the response. {assistant_name} answers with policy-grounded information from {company_name}'s internal documents - this is RAG (Retrieval-Augmented Generation) in action. She is not guessing; she is retrieving from the company's knowledge base.
// TODO: capture screenshot of borrower FHA answer (explore-borrower-fha.png)

. Now ask a question that triggers a tool call:
+
[source,text]
----
Can you estimate how much house I can afford on a $120,000 salary?
----
+
Notice the response includes precise numbers from the affordability calculator, not generic estimates with disclaimers. This is the difference between a chatbot (estimating) and an agent (calling a real tool and returning calculated results).
// TODO: capture screenshot of borrower affordability calculation (explore-borrower-affordability.png)

=== Try the CEO experience (David Park)

[start=8]
. Sign out of the Borrower persona. Click **Sign In** again, and this time select the **CEO** persona:
+
image::ceo-sign-in.png[]

. After signing in, you will land on the **Executive Dashboard**, the CEO persona's view of portfolio health and operations:
+
image::agentic-app5.png[CEO Executive Dashboard showing Pipeline Overview and Denial Analysis,link=self,window=blank,width=700]

. In the **Your Assistant** chat panel on the right, type the following question (if needed, adjust the center divider to reveal the chat panel or select the chat icon in the bottom right corner):
+
[source,text]
----
Show me the current pipeline status
----
+
The assistant responds with a detailed portfolio health overview: active applications, stage breakdown, pull-through rate, and average days to close:
+
image::agentic-app6.png[CEO Assistant responding with portfolio health overview,link=self,window=blank,width=700]

=== Build a conversation for later analysis

Before we move on, let's generate a richer session with multiple conversation exchanges (called "turns" in AI terminology - each turn is one question from you and one response from the agent). This will give us more data to explore when we get to tracing in section 4.1.

[start=11]
. In the same chat, ask a follow-up question:
+
[source,text]
----
What are the denial trends?
----

. Then ask one more:
+
[source,text]
----
How are my loan officers doing?
----

Each question triggers a different tool call behind the scenes. By the time you finish, your CEO session will have 3 turns spanning pipeline health, denial analysis, and officer performance. In section 4.1, you will trace every step of this conversation end-to-end.

=== What just happened?

The responses look great. But stop and think about what happened behind the scenes:

* Which **agent** processed your question? Was it a single agent, or did multiple agents collaborate?
* Which **tools** did the agent invoke to gather pipeline data, denial rates, and performance metrics?
* How **long** did each step take? Was the LLM call fast, or did a tool call add latency?
* What if the response was **wrong**? How would you trace back to the root cause?
* What if a tool call **failed silently**? Would you even know?

The platform captured all of it automatically. You will trace every step in section 4.1 (Agentic AI & Tracing).

== Exercise 3: Understand the architecture in context

Now that you have seen the platform running, let's connect what you observed to the architecture.

. Return to the **Terminal** tab. List all pods and their containers:
+
[source,bash,role=execute,subs=attributes+]
----
oc get pods -n wksp-{user} -o custom-columns="NAME:.metadata.name,STATUS:.status.phase,CONTAINERS:.spec.containers[*].name"
----
+
You should see 5 core pods running. Here is what each one does:
+
[cols="1,2",options="header"]
|===
|Component |Purpose

|mortgage-ai-api
|**One FastAPI pod** containing all 5 agent implementations, routes by persona via WebSocket

|mortgage-ai-ui
|React frontend for interacting with agents through persona-specific interfaces

|mortgage-ai-db
|PostgreSQL with pgvector for document storage and RAG embeddings

|minio
|S3-compatible object storage for uploaded documents

|keycloak
|Identity provider for persona-based authentication and role-based access control
|===
+
[#api-endpoints]
**API endpoints (all served by the same `mortgage-ai-api` pod):**
+
[source,text]
----
/api/prospect/chat    - Prospect Agent (initial inquiries)
/api/borrower/chat    - Borrower Agent (application intake)
/api/loanofficer/chat - Loan Officer Agent (pipeline management)
/api/underwriter/chat - Underwriter Agent (risk assessment)
/api/ceo/chat         - Executive Agent (analytics)
----

. Review the architecture diagram below. Each pod you just listed maps to a component in this diagram:
+
[#fed-aura-platform-architecture]
.{company_name} AI platform architecture
image::fed-aura-platform-architecture.svg[Diagram showing {assistant_name} connected to persona agents, self-hosted models, MCP tools, a RAG vector database, MLflow observability, and platform security layers,align=center]
+
The platform is built around a set of cooperating components. At the center is {assistant_name} - a set of persona-specific agents orchestrated with LangGraph and powered by self-hosted models served through vLLM. Each agent has access to domain knowledge via RAG, can call real tools via MCP, and is observable through MLflow tracing. All inference goes through the MaaS gateway, which enforces access control and usage metering. NeMo Guardrails filter inputs and outputs at the boundary. OpenShell sandboxes agent execution with deny-by-default policies.

**Mapping pods to architecture:**

[cols="1,2,2",options="header"]
|===
|Pod |Architecture Role |What it does

|mortgage-ai-api
|Agent Layer (center)
|Single FastAPI pod with 5 LangGraph agent implementations. Routes requests to the right agent based on persona. Makes tool calls via MCP, queries the database, calls the LLM.

|mortgage-ai-ui
|Frontend
|React application. Renders the persona-specific interfaces (Borrower portal, CEO dashboard, etc.)

|mortgage-ai-db
|Data Layer
|PostgreSQL with pgvector. Stores application data, conversation history, and vector embeddings for RAG.

|minio
|Storage Layer
|S3-compatible object storage. Holds uploaded documents (borrower PDFs, policy files) for document processing.

|keycloak
|Identity Layer
|OIDC identity provider. Authenticates users and maps them to personas with role-based access control.
|===

**It is not just the LLM.** A production AI system involves many components working together. The LLM is one part of a larger pipeline that includes data retrieval, tool execution, identity management, and observability. The architecture layers map directly to the workshop sections:

[cols="1,2,2",options="header"]
|===
|Architecture Layer |Components |Workshop Sections

|Inference
|vLLM, llm-d, MaaS
|2.1 Model Catalog, 2.4 Models as a Service

|Data
|pgvector, RAG, Prompt Registry
|3.1 System Prompts, 3.2 RAG, 3.3 AutoRAG

|Agentic
|LangGraph, MCP, MLflow
|4.1 Agentic AI & Tracing, 4.2 MCP, 4.3 Evaluation

|Security
|NeMo Guardrails, Garak, OpenShell
|5.1 AI Safety, 5.2 Guardrails & Adversarial Testing, 5.3 OpenShell Agent Sandboxing
|===

[%collapsible]
.Architecture deep-dives: focused diagrams per layer (click to expand)
====
Each section zooms in on one layer of this architecture. You can refer back to these diagrams at any time.

[cols="1,1,2",options="header"]
|===
|Focus Area |Diagram |Relevant Sections

|Self-hosted models
|image:fed-aura-platform-architecture-models-focus.svg[Models focus,width=200,link=self,window=blank]
|2.1 Model Catalog, 2.4 Models as a Service

|MaaS Gateway
|image:fed-aura-platform-architecture-maas-focus.svg[MaaS focus,width=200,link=self,window=blank]
|2.4 Models as a Service

|Prompt Registry
|image:fed-aura-platform-architecture-prompt-registry-focus.svg[Prompt Registry focus,width=200,link=self,window=blank]
|3.1 System Prompts

|RAG Pipeline
|image:fed-aura-platform-architecture-rag-focus.svg[RAG focus,width=200,link=self,window=blank]
|3.2 RAG, 3.3 AutoRAG
|===
====

IMPORTANT: You do not need to understand every component now. Each section focuses on one layer. This exercise gives you the map so you know where each section fits in the bigger picture.

== Section summary

**What you accomplished:**

* Logged in to OpenShift and verified all platform components are running and healthy
* Confirmed API and database health via the health endpoint
* Interacted with {assistant_name} as Prospect, Borrower, and CEO, observing how responses change by persona
* Connected running pods to the platform architecture diagram

**Key takeaways:**

* The mortgage-ai system runs as a single FastAPI service with 5 LangGraph agents, each serving a different persona with different tools and permissions
* Every request is automatically traced by MLflow, capturing the full decision path from input to output
* The platform is built from cooperating components - inference, data grounding, tool access, observability, and security - each addressed by a dedicated section in this workshop

**Next steps:**

You have explored the running platform and seen what it does. Starting with section 2, you will rebuild understanding of each layer, beginning with self-hosted model inference.
