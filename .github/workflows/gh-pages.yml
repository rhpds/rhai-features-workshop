name: github pages

on:
  push:
    branches: [main]
    paths:
      - 'content/**'
      - 'default-site.yml'
      - 'site.yml'
      - 'package.json'
      - '.github/workflows/gh-pages.yml'
  pull_request:
    branches: [main]
    paths:
      - 'content/**'
      - 'default-site.yml'
      - 'site.yml'
      - 'package.json'
      - '.github/workflows/gh-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: install antora
        run: npm i -g @antora/cli@3.1 @antora/site-generator@3.1 @antora/lunr-extension
      # site.yml is the playbook Showroom renders in-cluster. Building it here
      # keeps the lab from breaking in RHDP when only GitHub Pages was tested.
      - name: validate showroom playbook (site.yml)
        run: antora --fetch site.yml
      - name: antora generate
        run: antora --fetch default-site.yml
      - name: upload pages artifact
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: www
  deploy:
    if: ${{ github.ref == 'refs/heads/main' && github.event_name != 'pull_request' }}
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
    - name: deploy github pages
      id: deployment
      uses: actions/deploy-pages@v4
