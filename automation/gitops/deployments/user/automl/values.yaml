username: user1
namespacePrefix: wksp

bucket: automl
region: wonderland
dataFile: cleaned_loan_data.csv
job:
  image: amazon/aws-cli
  backoffLimit: 4
  # Bump to re-run the upload; the Job name carries it, so a new version supersedes
  # the old Job and ArgoCD prunes the previous one.
  version: 1
