username: user1
namespacePrefix: wksp

image: quay.io/minio/minio:RELEASE.2023-06-19T19-52-50Z
# Injected from agnosticv (bootstrap-infra minio.*).
rootUser: minio
rootPassword: "REPLACE_ME"
storage:
  size: 20Gi
region: wonderland
routes:
  enabled: true
# RHOAI data connections created against this MinIO.
connections:
  # 'pipeline' is consumed by the user/dspa chart.
  - name: pipeline
    bucket: pipeline
  - name: automl
    bucket: automl
resources:
  requests:
    cpu: 20m
    memory: 100Mi
  limits:
    cpu: 250m
    memory: 1Gi
