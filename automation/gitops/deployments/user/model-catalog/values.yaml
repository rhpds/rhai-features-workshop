username: user1
namespacePrefix: wksp

# Each entry creates a ServingRuntime + InferenceService pair.
# NOTE: these are CPU vLLM pods -- roughly 4-8 cores and 8-16Gi each. Keep the
# list short (or disable the whole component) unless the cluster is sized for it.
runtimeImage: registry.redhat.io/rhaiis/vllm-cpu-rhel9@sha256:f05e773647dddd37ec6c2215cb14bec87e4ab5a7d37f2e110d16cd92355427d3
maxModelLen: 2048
kvCacheSpace: "4"

models:
  - name: tinyllama
    displayName: TinyLlama/TinyLlama-1.1B-Chat-v1.0
    runtimeDisplayName: CUSTOM - vLLM CPU for TinyLlama
    templateName: vllm-cpu-tinyllama
    storageUri: oci://quay.io/rh-aiservices-bu/tinyllama:1.0
    enabled: true
    resources:
      requests:
        cpu: "4"
        memory: 8Gi
      limits:
        cpu: "8"
        memory: 8Gi
  - name: qwen3-06b
    displayName: Qwen/Qwen3-0.6B
    runtimeDisplayName: CUSTOM - vLLM CPU for Qwen3-0.6B
    templateName: vllm-cpu-qwen3-06b
    storageUri: hf://Qwen/Qwen3-0.6B
    enabled: false
    resources:
      requests:
        cpu: "4"
        memory: 16Gi
      limits:
        cpu: "8"
        memory: 16Gi
