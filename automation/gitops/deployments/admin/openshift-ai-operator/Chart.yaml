apiVersion: v2
name: openshift-ai-operator
description: Installs the Red Hat OpenShift AI operator (subscription only; the DataScienceCluster is owned by the openshift-ai chart)
type: application
version: 0.1.0
appVersion: "3.5"
