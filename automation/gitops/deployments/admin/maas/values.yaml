# Injected from bootstrap-infra values (remotellm.*), originally from agnosticv.
model:
  name: gpt-oss-120b
  # Hostname only, no scheme and no path.
  endpoint: maas-rhdp.apps.maas.redhatworkshops.io
  apiKey: "REPLACE_ME"
  apiFormat: openai-chat
  path: /v1/chat/completions
namespaces:
  externalModels: external-models
  aiTenants: ai-tenants
  tenant: models-as-a-service
gateway:
  name: maas-default-gateway
subscription:
  tokenLimit: 10000
  window: 1h
  priority: 10
