{{- if .Values.secretPatcher.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: patch-secret
  namespace: {{ .Values.namespace }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: patch-secret
  namespace: {{ .Values.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: edit
subjects:
  - kind: ServiceAccount
    name: patch-secret
    namespace: {{ .Values.namespace }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: update-secret-minio
  namespace: {{ .Values.namespace }}
spec:
  backoffLimit: 3
  template:
    spec:
      serviceAccountName: patch-secret
      restartPolicy: Never
      containers:
        - name: patch
          image: {{ .Values.secretPatcher.image }}
          command:
            - /bin/bash
            - -c
            - |
              echo "Waiting for secret ds-pipeline-s3-dspa..."
              for i in $(seq 1 60); do
                if oc get secret ds-pipeline-s3-dspa -n {{ .Values.namespace }} 2>/dev/null; then
                  break
                fi
                echo "  attempt $i/60 - not found yet, sleeping 10s..."
                sleep 10
              done

              if ! oc get secret ds-pipeline-s3-dspa -n {{ .Values.namespace }} 2>/dev/null; then
                echo "ERROR: secret ds-pipeline-s3-dspa not found after 10 minutes"
                exit 1
              fi

              ACCESS_KEY=$(oc get secret ds-pipeline-s3-dspa -n {{ .Values.namespace }} -o jsonpath='{.data.accesskey}' | base64 -d)
              SECRET_KEY=$(oc get secret ds-pipeline-s3-dspa -n {{ .Values.namespace }} -o jsonpath='{.data.secretkey}' | base64 -d)

              oc patch secret ds-pipeline-s3-dspa -n {{ .Values.namespace }} --type=merge -p "{
                \"stringData\": {
                  \"AWS_ACCESS_KEY_ID\": \"${ACCESS_KEY}\",
                  \"AWS_SECRET_ACCESS_KEY\": \"${SECRET_KEY}\",
                  \"AWS_S3_ENDPOINT\": \"http://minio-dspa.{{ .Values.namespace }}.svc.cluster.local:9000\",
                  \"AWS_S3_BUCKET\": \"mlpipeline\",
                  \"AWS_DEFAULT_REGION\": \"us-east-1\"
                }
              }"
              echo "Secret patched successfully"
{{- end }}
